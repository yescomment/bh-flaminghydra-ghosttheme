{{!< default}}

<main class="site-main">

    {{#tag}}
    <section class="tag">

        <div class="gh-canvas">

            {{#has tag="issues"}}
            <h1 class="tag-name">Every Issue of Flaming Hydra</h1>
            {{#else}}
            <h1 class="tag-name">{{name}}</h1>
            {{/has}}

            {{#if description}}
                <div class="tag-description">{{description}}</div>
            {{/if}}

            {{#if feature_image}}
                <div class="tag-media u-placeholder horizontal kg-width-wide">
                    <img class="u-object-fit" srcset="{{> srcset}}" src="{{img_url feature_image size="l"}}" alt="{{name}}">
                </div>
            {{/if}}

        </div>

    </section>
    {{/tag}}

    <div class="post-feed gh-feed gh-canvas">
        {{#foreach posts}}
            {{> "loop"}}
        {{/foreach}}
    </div>

    {{pagination}}

</main>